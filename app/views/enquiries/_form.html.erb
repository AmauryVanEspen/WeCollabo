<%= simple_form_for(@enquiry) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :email %>
    <%= f.input :enquiry_type, collection: ['Event space enquiry', 'Coworking enquiry', 'Event enquiry', 'Website error', 'Other'] %>
    <%= f.input :message %>
    <%= f.input :phone %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
